{
  "name": "rag-eval-figures",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "House-style figure toolchain: CC0 icon set + compositor that assembles specs into consistent SVG + PNG diagrams. PNG rasterization uses rsvg-convert (librsvg) — no Playwright/Chromium.",
  "scripts": {
    "icons": "node assets/render_pngs.mjs assets",
    "figures": "node build/build_figures.mjs",
    "build": "npm run icons && npm run figures"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}
