# Web Search Skill Environment Variables
# Copy this file to .env and set your actual bearer token

# Method 1: Set bearer token directly (fallback if credgoo not available)
# WEB_SEARCH_BEARER=your_bearer_token_here

# Method 2: Use credgoo for secure credentials management (recommended)
# Install: uv pip install -r https://skale.dev/credgoo
# Then store your key:
#   credgoo web-search --token YOUR_CREDGOO_TOKEN --key YOUR_ENCRYPTION_KEY
#
# The skill will automatically retrieve the token from credgoo if available.
