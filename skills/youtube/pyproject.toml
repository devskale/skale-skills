[project]
name = "youtube-skill"
version = "1.1.0"
description = "Search YouTube for fresh, long, deep content via Invidious API with smart ranking"
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
