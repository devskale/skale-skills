[project]
name = "youtube-skill"
version = "1.0.0"
description = "Search YouTube videos via Invidious API"
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
