@echo off
REM surf is macOS-only — it drives Google Chrome via AppleScript.
REM There is no Windows support. This stub exists so `pi install` does not fail.
echo.
echo surf is macOS-only.
echo It controls Google Chrome through macOS AppleScript, which has no Windows equivalent.
echo.
exit /b 1
